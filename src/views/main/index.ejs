<%- include('../partials/header.ejs') %>
   
    <div class="table-responsive">
        <table class="table-product-car table table table-hover">
            <thead>
                <tr>
                    <th scope="col" style="text-align: center;">Foto de Perfil</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Apellido </th>
                    <th scope="col">Email</th>
                    <th scope="col">Acciones</th>

                </tr>
            </thead>
            <tbody>
                <%  users.forEach(user => { %>
                <tr>
                    <td scope="row1" style="width: 50%;"><img src="/images/<%= user.image %>" alt=" image"
                            class="iroman-img"></i></a></td>
                    <td class="name"><%= user.name %></td>
                    <td class="surname"><%= user.lastname %></td>
                    <td class="mail"><%= user.email %></td>
                    <td class="acctions">
                        <a href="/products/detailProduct/<%= user.id %>">Ver Detalle</a><br>
                        <a href="/products/detailProduct/<%= user.id %>">Editar</a><br>
                        <a href="/products/detailProduct/<%= user.id %>">Elminar</a>
                    </td>
                </tr>
                <% }) %>

            
        </table>

    </div>
    <%- include('../partials/footer.ejs') %>